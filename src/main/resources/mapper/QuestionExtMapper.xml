<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="life.lv.community.mapper.QuestionExtMapper">
    <resultMap id="BaseResultMap" type="life.lv.community.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Thu Aug 29 16:23:02 CST 2019.
        -->
        <id column="id" jdbcType="BIGINT" property="id" />
        <result column="title" jdbcType="VARCHAR" property="title" />
        <result column="gmt_create" jdbcType="BIGINT" property="gmtCreate" />
        <result column="gmt_modified" jdbcType="BIGINT" property="gmtModified" />
        <result column="creator" jdbcType="BIGINT" property="creator" />
        <result column="view_count" jdbcType="INTEGER" property="viewCount" />
        <result column="like_count" jdbcType="INTEGER" property="likeCount" />
        <result column="tag" jdbcType="VARCHAR" property="tag" />
        <result column="comment_count" jdbcType="INTEGER" property="commentCount" />
    </resultMap>
    <resultMap extends="BaseResultMap" id="ResultMapWithBLOBs" type="life.lv.community.model.Question">
        <!--
          WARNING - @mbg.generated
          This element is automatically generated by MyBatis Generator, do not modify.
          This element was generated on Thu Aug 29 16:23:02 CST 2019.
        -->
        <result column="description" jdbcType="LONGVARCHAR" property="description" />
        <result column="like_used" jdbcType="LONGVARCHAR" property="likeUsed" />
    </resultMap>

    <select id="countBySearch" parameterType="life.lv.community.dto.QuestionQueryDTO"
            resultType="java.lang.Integer">
        select count(*) from question
        <where>
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>
            <if test="tag != null and tag != ''">
                and tag regexp #{tag}
            </if>
            <if test="sort != null and sort != '' and sort == 'no'">
                and comment_count = 0
            </if>
            <if test="time != null and time != ''">
                and gmt_create > #{time}
            </if>
        </where>
    </select>

    <select id="selectBySearch" parameterType="life.lv.community.dto.QuestionQueryDTO"
            resultMap="BaseResultMap">
        select * from question
        <where>
            <if test="search != null and search != ''">
                and title regexp #{search}
            </if>
            <if test="tag != null and tag != ''">
                and tag regexp #{tag}
            </if>
            <if test="sort != null and sort != '' and sort == 'no'">
                and comment_count = 0
            </if>
            <if test="time != null and time != ''">
                and gmt_create > #{time}
            </if>
        </where>
        <if test="sort == null or sort == ''">
            order by gmt_create desc
        </if>
        <if test="sort != null and sort != '' and sort == 'new'">
            order by gmt_create desc
        </if>
        <if test="sort != null and sort != '' and sort == 'no'">
            order by gmt_create desc
        </if>
        <if test="sort != null and sort != '' and (sort == 'hot' || sort == 'hot7' || sort == 'hot30')">
            order by comment_count desc
        </if>
        limit #{page},#{size}
    </select>
</mapper>